# Paper Artifacts

This folder collects key figures and reports generated by the code to include in the paper. Files are grouped by setting.

## Stratified (3‑class, development)
- confusion_matrix.png — confusion matrix (file‑wise)
- f1_per_class.png — per‑class F1 bars
- report.txt — full classification report (3 classes)
- report_present.txt — report over present labels
- train_curves.png, train_loss_curve.png, val_metrics_curve.png — training/evaluation curves

Suggested numbers to cite (auto_r22): Macro‑F1 ≈ 0.776; F1(healthy≈0.69, degrading≈0.64, fault=1.00).

## Temporal (deployment, TTF split)
- confusion_matrix.png, f1_per_class.png, report.txt, report_present.txt
- report_early_70_90.txt, report_late_90_100.txt — stage‑specific reports
- time_metrics.png — accuracy/macro‑F1 vs. TTF bins
- class_dist_over_ttf.png — class distribution vs. TTF
- time_metrics.csv — raw bin metrics (for tables)

Suggested numbers to cite (2‑class test slice): Acc ≈ 0.95; Macro‑F1(2 classes) ≈ 0.947; F1(degrading≈0.93, fault≈0.96). If test widened to 70–100% TTF, describe class coverage clearly.

## Temporal (all‑TTF, overall view — for appendix only)
- confusion_matrix.png, f1_per_class.png, report.txt, report_present.txt

Suggested numbers to cite (overall): Acc ≈ 0.69; Macro‑F1 ≈ 0.589; F1(healthy≈0.80, degrading≈0.05, fault≈0.92). Not a leakage‑free test; use for trend illustration only.

## Re‑generate
- Stratified: run `train_logs.py` + `eval_logs.py` with `configs/logs_stft_train_strat.yaml`.
- Temporal: run `train_logs.py` + `eval_logs.py` with `configs/logs_stft_full_temporal.yaml` and plot via `scripts/plot_time_metrics.py`.
